strategy: orb_continuation
family: orb
version: strategy_v1
required_feature_set: opening_core_v1
signal_contract_version: signal_v1

features:
  feature_config: configs/features/opening_core_v1.yaml

signal:
  # Phase19 immediate fix: canonical configs use side_mode (side is legacy).
  side_mode: long_only
  orb_open_minutes: 15
  entry_start_minute: 20
  entry_end_minute: 180
  require_close_above_vwap: false
  min_vwap_slope: 0.0
  min_orb_width_pct: 0.001
  max_orb_width_pct: 0.05

risk:
  stop_mode: orb_mid
  target_mode: fixed_r
  target_r: 1.25
  atr_buffer_mult: 0.35
  min_risk_per_share: 0.03
  max_trades_per_day: 1

backtest:
  quantity: 1.0
  commission_per_trade: 0.0
  slippage_per_share: 0.01
  eod_exit_minute: 389
  max_hold_minutes: 60
