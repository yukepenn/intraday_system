# configs/strategies/grids/phase19_immediate_fix_current10_side_aware/

Inspect-only current-10 side-aware branch grids. They exist to verify bounded
`signal.side_mode` grid resolution and setup-code/config consistency.

They are not proof of short alpha, not candidate input, and not authorization
for actual Layer1 economic grids.
