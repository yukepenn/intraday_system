# configs/layer3/

Layer3 frozen-validation configs:

- `folds_*.yaml` — fold time windows.
- `fixed_system_*.yaml` — frozen system reference (candidate root, Layer2 config, execution config, folds, git sha).

Layer3 is locked until Layer2 frozen systems exist. Phase19 polish does not
authorize WFO, frozen validation, live, or paper configs.
