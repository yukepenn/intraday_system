# Status / doc hygiene (Phase 6d Phase 1)

Small documentation updates **only** — no runtime behavior changes.

See `status_doc_hygiene.csv` for file-by-file actions.

Themes:

1. Phase **6c** grid review artifacts remain the quantitative source; Phase **6d** adds parameterized diagnostics without re-running grids.
2. **Candidate selection** and **YAML promotion** stay unimplemented by explicit policy (see `NEXT_HANDOFF.md`).
3. Stale changelog **Decision** tail in `CHANGES.md` corrected so it cannot be mistaken for project direction.
