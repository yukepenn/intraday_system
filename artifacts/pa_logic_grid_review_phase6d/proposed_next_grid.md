# Proposed diagnostic grids — **proposal only**

No grids were executed in Phase **6d** per task mandate.

Each row in **`proposed_next_grid.csv`** enumerates optional **narrow** supplemental sweeps justified by unresolved economic questions **without** escalating to broad research modes.

Ranking guidance:

| Priority | Proposal | Reason |
| --- | --- | --- |
| P0 (process) | **PG5 replication windows** framed as methodological requirement rather than exploratory whim | Addresses sample uniqueness / argmax leakage at design layer |
| P1 | **PG1 atr_buffer diagnostic** | Complements intrinsic structural stops already implemented |
| P2 | **PG2 payoff ladder narrowing** | Target spacing remains undersampled |
| P3–P4 | Micro hygiene (`close_position_min`, horizon) | Only after overarching selection policy defines tolerances |

**None** proposals **block** authoring the selection doctrine file—unless future governance explicitly demands cross-window evidence before prose sign-off (**then PG5 obtains priority** logically, still not mandated as automatic code execution now).
