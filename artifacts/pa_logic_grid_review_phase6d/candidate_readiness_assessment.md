# Candidate-readiness assessment — Phase 6d

Formal label (`candidate_readiness_assessment.csv`): **`READY_TO_DESIGN_SELECTION`**

This label means the project may proceed to **`DESIGN_LAYER1_PA_CANDIDATE_SELECTION`** (documentation + scaffolding requirements) — **without** asserting:

- statistically durable alpha,
- production readiness,
- or immediate YAML promotion privileges.

See CSV rows for per-dimension adjudication (`trade_count_adequacy`, `performance_stability`, `drawdown_risk`, `parameter_sensitivity`, `artifact_completeness`, `reconstruction_promotion_risk`, `selection_process_risk`, `overall_label`).

## Why design is permissible now

1. Diagnostic artifacts reconcile **engineering truth** (`config_hash`, `TradeResult` aggregates) with **economics-informed commentary**.
2. Dominant causal structure (**stop mode partition**) is unmistakable → future selection policies can encode **tiered skepticism**.
3. **Promotion blockers** (full resolved serialization) are identifiable **ahead** of YAML authoring.

## What remains insufficient for promotion

- Multi-window corroboration
- Explicit resolved-config archival per combo (recommended schema augmentation)
- Broader ladders on risk (`atr_buffer`), targets, horizons — **orthogonal** backlog items
