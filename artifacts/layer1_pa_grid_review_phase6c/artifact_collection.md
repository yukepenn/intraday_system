# Artifact collection (sanitized review copy)

Source grid outputs were produced under repo-relative `artifacts/layer1_pa_controlled_grid_phase6b/local_run/`. Committed Phase 6c review copies live under `artifacts/layer1_pa_grid_review_phase6c/` with **no absolute paths** embedded in these tables.

See `artifact_collection.csv` for path mapping and row counts.
