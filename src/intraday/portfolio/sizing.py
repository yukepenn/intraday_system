"""Sizing helpers (skeleton)."""

from __future__ import annotations


def fixed_quantity(qty: float) -> float:
    """Return a fixed quantity. Placeholder; Phase 8/9 will expand."""
    return float(qty)
