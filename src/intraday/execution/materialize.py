"""Materialize stops/targets from intents (skeleton)."""

from __future__ import annotations

from intraday.core.errors import IntradaySystemError


def materialize_stop_target(*args, **kwargs):
    """Compute materialized stop/target prices from intent + entry price. (Phase 2)"""
    raise IntradaySystemError("materialize_stop_target is not implemented yet (Phase 2).")
